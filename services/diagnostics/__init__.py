"""Request inspection and diagnostics layer.

Provides :class:`RequestInspector`, :class:`ResponseInspector` and
:class:`DiagnosticsRecorder` so every HTTP interaction with TikTok can be
observed and recorded without altering the request itself.
"""
